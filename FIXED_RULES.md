# 고정불변 전망 원칙

1. 공식·시장·거시 자료를 가능한 범위에서 최대한 결합한다.
2. 다른 엔진이 이미 계산한 값은 재계산하지 않고 재사용한다.
3. 무료 API만 사용하되 장기 이력, 워크포워드 백테스트, 실제 오차 기반 구간을 적용한다.
4. 입력값 날짜 정렬·중복·단위·비정상치 검증에 실패하면 신뢰도를 자동 하향한다.
5. 충분한 표본이 없을 때 기관급이라는 표현을 사용하지 않는다.
6. 전망값은 확정값이 아니라 확률·구간·신뢰도와 함께 제공한다.
7. 기능 정확성을 유지하면서 외부 API·UrlFetch·GitHub 호출을 최소화하고, 중복 호출 방지·캐싱·배치 처리·로컬 계산을 우선 적용해 서버 부하를 최소화한다.
