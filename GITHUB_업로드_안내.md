# GitHub 업로드용 한국엔진

이 폴더는 GitHub 파일 수 제한을 고려한 업로드용 전체 한국엔진입니다.

- `__pycache__`와 `.pyc`는 Python 실행 시 자동 생성되는 파일이므로 제외했습니다.
- `output/vintages/`는 과거 일자별 중복 산출물이므로 제외했습니다.
- 현재 대시보드가 읽는 `output/*.json`과 테스트·설정·소스 코드는 포함했습니다.
- `.gitignore`가 실행 찌꺼기와 빈티지 산출물이 다시 Git에 추가되는 것을 방지합니다.

업로드 전 파일 수는 100개 이하인지 한 번 확인하고, 새 실행 뒤에는 `output/*.json`만 갱신해 커밋합니다.
